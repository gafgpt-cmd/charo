name: checks

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scripts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Shell scripts are found by shebang rather than a hard-coded list, so a
      # newly added script is checked without editing this workflow.
      - name: ShellCheck
        run: |
          set -eu
          files=$(grep -rl '^#!/bin/sh' .local/bin .config/git/hooks || true)
          if [ -z "$files" ]; then
            echo "no shell scripts found - this workflow is looking in the wrong place"
            exit 1
          fi
          echo "$files"
          # shellcheck disable=SC2086
          shellcheck -S style $files

      - name: Python syntax
        run: |
          set -eu
          files=$(grep -rl '^#!/usr/bin/env python3' .local/bin || true)
          for file in $files; do
            echo "$file"
            python3 -m py_compile "$file"
          done

      - name: Executable bits
        run: |
          set -eu
          status=0
          for file in $(grep -rl '^#!' .local/bin .config/git/hooks || true); do
            if [ ! -x "$file" ]; then
              echo "not executable: $file"
              status=1
            fi
          done
          exit "$status"
